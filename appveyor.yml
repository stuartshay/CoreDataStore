version: '1.0.{build}'

branches:  
 only:
  - core-net-1-0

before_build:
- cmd: >-
  npm install -g npm3
  npm3 install -g typings
 
build_script:
- cmd: >-
  dotnet restore -v debug
  dotnet build

test: off
